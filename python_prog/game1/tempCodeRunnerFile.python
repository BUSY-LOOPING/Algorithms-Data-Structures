import numpy as np
import matplotlib.pyplot as plt

# make some data and plot it
N = 100
X = np.linspace(0, 6 * np.pi, N)
Y = np.sin(X)

n = len(X)
data = [np.ones(n)]

for d in range(2) :
    data.append(X ** (d+1))
    
a = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
b = [[[10, 20, 30], [40, 50, 60], [70, 80, 90]]]
a = np.vstack([a, np.ones(3)]).T
print(a)
